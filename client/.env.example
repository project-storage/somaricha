# ==============================================================================
# SOMARICHA - FRONTEND CLIENT ENVIRONMENT TEMPLATE (.env.example)
# ==============================================================================

# Backend API Endpoint URL
# Note: Vite custom environment variables MUST start with the prefix "VITE_".
# In production, replace this with your hosted API endpoint (e.g., https://api.somaricha.com/api)
VITE_API_URL=http://localhost:3000/api
